# 任务拆解：voucher-update

## 1. 基础设施准备
- [x] 1.1 引入 RabbitMQ 相关依赖与配置，补充 `application.yml` 中的连接、监听、ACK 与确认参数。
- [x] 1.2 新增 RabbitMQ 基础配置类，声明秒杀交换机、队列、路由键、死信交换机与死信队列。
- [x] 1.3 新增雪花算法订单 ID 生成器，并明确 workerId、起始时间戳与时钟回拨保护策略。

## 2. 秒杀主链路改造
- [x] 2.1 调整 `VoucherOrderService` 与 `VoucherOrderServiceImpl`，在抢券入口生成雪花算法订单 ID。
- [x] 2.2 修改 Lua 脚本入参与返回码，继续承担库存校验、一人一券判定与 Redis 扣减。
- [x] 2.3 在 Lua 执行成功后发送 RabbitMQ 下单消息，失败时输出中文错误日志并返回统一繁忙提示。
- [x] 2.4 保留并修正 Redis 预热与 key 生命周期逻辑，确保库存 key 和用户集合 key 与活动生命周期一致。

## 3. 异步消费与落库
- [x] 3.1 新增秒杀下单消息体定义，包含 `orderId`、`userId`、`voucherId`。
- [x] 3.2 新增 RabbitMQ 消费者，使用手动 ACK 模式消费秒杀订单消息。
- [x] 3.3 在消费者中保留 Redisson 用户级锁，防止同一用户重复消息并发落库。
- [x] 3.4 为 `VoucherMapper` 增加数据库原子扣库存能力，确保 `stock > 0` 时才允许扣减。
- [x] 3.5 在消费者中完成数据库扣库存、订单创建、ACK、异常重试与死信处理。
- [x] 3.6 利用数据库唯一索引与代码层幂等处理，兜底重复消费场景。

## 4. 数据结构与模型调整
- [x] 4.1 调整 `VoucherOrder` 主键写入策略，使业务生成的雪花算法 ID 可直接插入。
- [x] 4.2 复核 `tb_voucher_order` 唯一索引与表结构，确保与异步幂等逻辑一致。
- [ ] 4.3 如有需要，补充 SQL 初始化脚本或迁移说明，反映订单 ID 与异步下单规则的变化。

## 5. 测试与验证
- [x] 5.1 为 Lua 抢券结果分支补充单元或集成测试，覆盖库存不足、重复领取、活动失效等场景。
- [x] 5.2 为 RabbitMQ 消费者补充测试，覆盖正常消费、重复消费、库存不足、ACK/重试场景。
- [ ] 5.3 手工验证完整链路：Redis 预热 -> 抢券返回订单号 -> 异步落库成功。
- [ ] 5.4 手工验证异常链路：MQ 不可用、重复抢券、消费异常进入死信队列。

## 6. 收尾与可运维性
- [x] 6.1 为关键链路增加必要中文日志：订单号生成、MQ 发送、消费成功、消费失败、死信告警。
- [ ] 6.2 梳理关键 Redis key、RabbitMQ 队列与异常排查步骤，便于后续定位问题。
- [ ] 6.3 复核前端抢券成功/失败提示语义，确认是否沿用“抢券成功”文案或调整为“已受理”。
