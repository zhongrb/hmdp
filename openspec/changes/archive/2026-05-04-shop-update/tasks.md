# 任务拆解：shop-update

## 1. 缓存模型升级
- [x] 1.1 为 `RedisConstants` 补充商户热点重建锁 key，例如 `LOCK_SHOP_KEY`。
- [x] 1.2 新增逻辑过期缓存包装结构（如 `RedisData`），用于存储 `data + expireTime`。
- [x] 1.3 扩展 `CacheClient`：支持空值缓存、逻辑过期读取、互斥锁重建、异步重建线程池与 TTL 抖动写入。
- [x] 1.4 调整 `ShopServiceImpl.queryById()` 接入逻辑过期热点缓存模型，替换当前仅 pass-through 的详情查询链路。

## 2. 商户写接口补齐
- [x] 2.1 扩展 `ShopService` 接口，新增商户创建、更新、删除方法定义。
- [x] 2.2 扩展 `ShopController`，新增 `POST /shops`、`PUT /shops/{shopId}`、`DELETE /shops/{shopId}`。
- [x] 2.3 在 `ShopServiceImpl` 中实现商户新增逻辑：写 DB、写 GEO、删详情缓存。
- [x] 2.4 在 `ShopServiceImpl` 中实现商户更新逻辑：查旧值、更新 DB、按差异维护 GEO、删详情缓存。
- [x] 2.5 在 `ShopServiceImpl` 中实现商户删除逻辑：查旧值、删 DB、删 GEO、删详情缓存。

## 3. GEO 一致性维护
- [x] 3.1 提炼商户 GEO 新增/覆盖写入逻辑，统一维护 `shop:geo:{typeId}`。
- [x] 3.2 处理商户更新时 `typeId` 或 `x/y` 变化场景，避免附近商户结果残留旧索引。
- [x] 3.3 处理商户删除时的 GEO 清理，确保附近商户查询不再返回已删商户。

## 4. 异常处理与日志
- [x] 4.1 为热点缓存重建、锁竞争、空值写入增加必要中文日志，便于排查缓存击穿与雪崩问题。
- [x] 4.2 为商户增删改中的缓存删除与 GEO 维护增加必要中文日志。
- [x] 4.3 明确 Redis/GEO 操作失败时的异常策略：数据库成功后记录日志并向上抛出或降级处理。

## 5. 测试与验证
- [x] 5.1 为 `CacheClient` 或 `ShopServiceImpl` 补充测试，覆盖缓存命中、逻辑过期、异步重建、空值缓存场景。
- [x] 5.2 为商户新增、更新、删除补充服务层或集成测试，覆盖缓存删除与 GEO 维护行为。
- [ ] 5.3 手工验证商户详情热点查询：缓存过期窗口内仍可稳定返回，后台触发重建。
- [ ] 5.4 手工验证商户更新：修改名称/类型/坐标后，详情页与附近商户结果同步更新。
- [ ] 5.5 手工验证商户删除：详情查询返回不存在，附近商户不再出现该商户。

## 6. 收尾与文档
- [x] 6.1 梳理关键 Redis key：`cache:shop:{id}`、`lock:shop:{id}`、`shop:geo:{typeId}`。
- [x] 6.2 补充缓存热点重建与商户 GEO 异常排查说明，便于后续定位问题。
- [x] 6.3 复核是否需要补充前端或接口调用方对商户增删改接口的联调说明。
